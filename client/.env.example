# The client calls the API with relative paths ("/api/...").
# - Local dev: Vite proxies /api -> http://localhost:4000 (vite.config.ts).
# - Production: Vercel rewrites /api -> the Render backend (vercel.json).
# No API base URL env var is required.
